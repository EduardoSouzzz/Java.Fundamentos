public class MinhaClass_3 {
    public static void main(String[] args) {

        // Nomes Ordem Inversa;

        String[] nomes = {"Julia", "Marcelo", "Nessa", "Ireno", "Niceia"};

        for (int i = nomes.length - 1; i >= 0; i--) {
            System.out.println(nomes[i]);
        }
    }
}
